MODULE Test;
   TYPE
      T0 = POINTER TO OBJECT END;
      T1 = POINTER TO OBJECT(T0) i: INTEGER END;
   VAR t : T1; v: T0;
BEGIN
  PRINTLN(v IS T1) // FALSE because v is NIL

  NEW(t);
  v := t;
  CASE v OF
  | T0: PRINTLN("T0")
  | T1: PRINTLN("T1")
  ELSE
    PRINTLN("NIL")
  END
END Test.
